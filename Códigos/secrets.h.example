#pragma once

// Copie este arquivo para "secrets.h" e preencha com os dados da sua rede.
// secrets.h fica de fora do git (veja .gitignore) -- nunca commite senha de wifi.
const char* WIFI_SSID = "NOME_DA_SUA_REDE";
const char* WIFI_PASS = "SENHA_DA_SUA_REDE";
